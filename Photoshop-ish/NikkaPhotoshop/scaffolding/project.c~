//
//
#include <stdio.h>
int main(int argc, char* argv[]) {
  FILE * input = fopen(argv[1],"r");
  //read_ppm(input);

  //temporary for testing
  char a = 'a';
  int b = 0;
  int c = 0;
  int d = 0;
  int numCollected = fscanf(input, "%c", &a);
  fscanf(input, "%d", &b);
  // fscanf(input, "%d%d%d", &b, &c, &d);

  return 0;
}
